<span class="inner-field-short-description">

    {% if object and admin.hasRoute('edit') and admin.isGranted('EDIT') %}
    	<span>
			{% if object.providerName is defined and object.providerName == "sonata.media.provider.image" %}
				
				<a href="{{ admin.generateObjectUrl('edit', object, link_parameters) }}" target="new">
					{% media object, "small" %}
					<span>{{ description }}</span>
				</a>
			{% else %}
				<a href="{{ admin.generateObjectUrl('edit', object, link_parameters) }}" target="new">{{ description }}</a>
			{% endif %}
		</span>
        {# <a href="{{ admin.generateObjectUrl('edit', object, link_parameters) }}" target="new">{{ description }} /yolo</a> #}
    {% else %}
        {{ description }}
    {% endif %}
</span>